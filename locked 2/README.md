# Locked 2 Workspace

Current Locked 2 work-in-progress files collected in one place.

## Files

- `locked2_shift_sprint.lua` - current Linoria Shift Sprint script with Flow awareness, slide protection, and measurement guard.
- `locked2_slide_analyzer.lua` - Linoria slide/dash analyzer and logger.
- `locked_2.lua` - existing client game script from `client/games`.
- `locked_2_diagnostics.lua` - existing diagnostics script from `client/diagnostics`.

## Notes

- `locked2_shift_sprint.lua` is the active sprint/Flow script we are iterating on now.
- `locked2_slide_analyzer.lua` is for research/logging only.
- The files here are copies collected for GitHub organization; keep the active script in sync when making new changes.
